# Passcode gating /tours/* (property video tours) — see middleware.ts and
# lib/gate.ts. Required in every environment; with it unset the gate fails
# closed (nothing can unlock), so set a real value locally and in the deploy
# platform's env vars. Never commit an actual passcode here.
DRONE_HUB_PASSCODE=
