// Public export surface for <FileUpload> — import from
// "@/components/FileUpload" (or copy this folder into a standalone
// consumer like personal-site — everything it transitively imports is
// scoped to this folder, no app/ or gating deps, no network/storage/auth
// logic of any kind — see FileUpload.tsx's header comment).
export { FileUpload } from "./FileUpload";
export type { FileUploadProps } from "./FileUpload";
