// Public export surface for <Gallery> — import from "@/components/Gallery"
// (or copy this folder into a standalone consumer like personal-site — its
// only dependency is embla-carousel-react + lucide-react, no app/ or
// gating deps, no network/storage/auth logic of any kind — see
// Gallery.tsx's header comment).
export { Gallery } from "./Gallery";
export type { GalleryProps, GalleryImage } from "./Gallery";
